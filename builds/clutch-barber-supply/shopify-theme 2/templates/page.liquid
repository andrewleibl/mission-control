<div class="page-header">
  <h1>{{ page.title }}</h1>
</div>

<div class="page-content" style="max-width: 800px; margin: 0 auto; padding: 2rem 1.5rem;">
  {{ page.content }}
</div>

<style>
.page-content p { margin-bottom: 1rem; color: var(--clutch-text-muted); line-height: 1.7; }
.page-content h2 { font-size: 1.5rem; font-weight: 700; margin: 2rem 0 1rem; }
.page-content ul, .page-content ol { margin: 1rem 0; padding-left: 1.5rem; color: var(--clutch-text-muted); }
.page-content li { margin-bottom: 0.5rem; }
</style>